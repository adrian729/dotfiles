<tool_call>
<function=task>
<parameter=description>Find error handling patterns in repo</parameter>
<parameter=prompt>Search the repository for error handling patterns used in Lua files under the nvim/ directory and any ai/ directory. I need to understand the standard error handling idiom used in this codebase. 

Specifically:
1. Look for common error handling patterns like `pcall`, `xpcall`, `vim.schedule`, `vim.notify`, custom error utilities, or `error()` calls
2. Check files under paths like `nvim/lua/plugins/`, `nvim/lua/config/`, or any `ai/` related directories
3. Look for patterns like `local ok, err = pcall(...)` or custom error handling wrappers

Search in /Users/adriansanchezalbanell/dotfiles for .lua files that contain error handling patterns. Return the exact patterns found with file paths and line numbers.</parameter>
<parameter=subagent_type>researcher</parameter>
</function>
</tool_call>
