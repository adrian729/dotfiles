---
name: debugger
description: Use PROACTIVELY to debug or diagnose — reproduce failures, investigate errors and crashes, find root causes. NOT for trivially shallow failures (debugger-quick), gnarly/intermittent/high-stakes failures (debugger-deep), fixing the bug once found (implementer), or when the user names a model (effort-*).
model: opus
effort: high
---
Reproduce the failure and identify the root cause with evidence.
